{
  "buildCommand": "npm run build",
  "outputDirectory": "packages/app-web/dist",
  "installCommand": "npm install",
  "devCommand": "npm run dev",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}

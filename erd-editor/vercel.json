{
  "buildCommand": "npm run build --prefix packages/app-web",
  "outputDirectory": "packages/app-web/dist",
  "installCommand": "npm install --prefix packages/app-web",
  "devCommand": "npm run dev --prefix packages/app-web",
  "rewrites":  [
    {"source": "/(.*)", "destination": "/"}
  ]
}
